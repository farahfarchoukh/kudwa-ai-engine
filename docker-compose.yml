version: "3.9"
services:
  api:
    build: .
    container_name: kudwa_api
    volumes:
      - ./:/app
      - ./kudwa.db:/app/kudwa.db   # persistent SQLite file
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    ports:
      - "8000:8000"
    restart: unless-stopped

